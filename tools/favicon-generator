<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Canva-Ready Thread To Success Favicon</title>
    <style>
        body {
            font-family: 'Segoe UI', sans-serif;
            background: linear-gradient(135deg, #0f172a 0%, #1e293b 50%, #334155 100%);
            color: white;
            margin: 0;
            padding: 20px;
            min-height: 100vh;
        }
        
        .container {
            max-width: 800px;
            margin: 0 auto;
            padding: 40px;
            text-align: center;
        }
        
        .favicon-canvas {
            width: 512px;
            height: 512px;
            margin: 20px auto;
            border-radius: 20px;
            box-shadow: 0 20px 40px rgba(0,0,0,0.5);
        }
        
        .download-section {
            background: rgba(59, 130, 246, 0.1);
            border: 1px solid rgba(59, 130, 246, 0.3);
            border-radius: 12px;
            padding: 30px;
            margin: 30px 0;
        }
        
        .btn {
            background: #3b82f6;
            color: white;
            border: none;
            padding: 15px 30px;
            border-radius: 8px;
            cursor: pointer;
            font-weight: 600;
            font-size: 16px;
            margin: 10px;
            transition: background 0.3s ease;
        }
        
        .btn:hover {
            background: #2563eb;
        }
        
        .instructions {
            background: rgba(255,255,255,0.1);
            border-radius: 12px;
            padding: 30px;
            margin: 30px 0;
            text-align: left;
        }
        
        .step {
            margin: 15px 0;
            padding: 10px 0;
            border-bottom: 1px solid rgba(255,255,255,0.1);
        }
        
        .step:last-child {
            border-bottom: none;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>🧵 3C Thread To Success</h1>
        <h2>Canva-Ready Favicon Generator</h2>
        <p>Generate PNG versions of your thread favicon for Canva and other platforms</p>
        
        <!-- Canvas for generating favicon -->
        <canvas id="faviconCanvas" class="favicon-canvas" width="512" height="512"></canvas>
        
        <div class="download-section">
            <h3>📥 Download Your Favicon</h3>
            <p>Choose the size you need for your platform:</p>
            <button class="btn" onclick="downloadFavicon(512)">📱 512x512 (High-Res)</button>
            <button class="btn" onclick="downloadFavicon(256)">💻 256x256 (Standard)</button>
            <button class="btn" onclick="downloadFavicon(128)">🌐 128x128 (Web)</button>
            <button class="btn" onclick="downloadFavicon(64)">📄 64x64 (Small)</button>
            <button class="btn" onclick="downloadFavicon(32)">🔍 32x32 (Tiny)</button>
        </div>
        
        <div class="instructions">
            <h3>🎨 How to Add to Your Canva Website:</h3>
            
            <div class="step">
                <strong>Step 1:</strong> Download the <strong>256x256</strong> or <strong>512x512</strong> version above
            </div>
            
            <div class="step">
                <strong>Step 2:</strong> Go to your <strong>Canva Website Editor</strong>
            </div>
            
            <div class="step">
                <strong>Step 3:</strong> Look for <strong>"Settings"</strong> → <strong>"Favicon"</strong> or <strong>"Site Icon"</strong>
            </div>
            
            <div class="step">
                <strong>Step 4:</strong> Upload your downloaded PNG file
            </div>
            
            <div class="step">
                <strong>Step 5:</strong> <strong>Publish your site</strong> and your thread favicon will appear in browser tabs!
            </div>
        </div>
        
        <div class="instructions">
            <h3>🌐 For Custom Domains:</h3>
            
            <div class="step">
                <strong>If you have a custom domain:</strong> You might need to add the favicon to your domain's root directory as <code>favicon.ico</code>
            </div>
            
            <div class="step">
                <strong>For WordPress/Other CMS:</strong> Use the same PNG files in your admin dashboard's favicon settings
            </div>
            
            <div class="step">
                <strong>For direct HTML:</strong> Use the SVG code we generated earlier for your 3C Control Center
            </div>
        </div>
        
        <div class="download-section">
            <h3>🧵 Brand Consistency Achieved!</h3>
            <p><strong>Your thread favicon will now appear across:</strong></p>
            <p>✅ 3C Control Center Dashboard<br/>
            ✅ Canva Website<br/>
            ✅ Custom Domain (if applicable)<br/>
            ✅ Any other web properties</p>
            <p><em>Complete brand unity through the Thread To Success story!</em></p>
        </div>
    </div>

    <script>
        // Draw the connected threads favicon on canvas
        function drawFavicon() {
            const canvas = document.getElementById('faviconCanvas');
            const ctx = canvas.getContext('2d');
            
            // Clear canvas
            ctx.clearRect(0, 0, 512, 512);
            
            // Create gradient background
            const gradient = ctx.createLinearGradient(0, 0, 512, 512);
            gradient.addColorStop(0, '#3b82f6');
            gradient.addColorStop(1, '#1e40af');
            
            // Draw rounded rectangle background
            ctx.fillStyle = gradient;
            ctx.beginPath();
            ctx.roundRect(0, 0, 512, 512, 60);
            ctx.fill();
            
            // Draw connected threads
            ctx.strokeStyle = 'white';
            ctx.lineWidth = 12;
            ctx.lineCap = 'round';
            
            // First thread line
            ctx.beginPath();
            ctx.moveTo(60, 150);
            ctx.quadraticCurveTo(256, 100, 452, 150);
            ctx.quadraticCurveTo(256, 200, 60, 150);
            ctx.stroke();
            
            // Second thread line
            ctx.beginPath();
            ctx.moveTo(60, 256);
            ctx.quadraticCurveTo(256, 206, 452, 256);
            ctx.quadraticCurveTo(256, 306, 60, 256);
            ctx.stroke();
            
            // Third thread line
            ctx.beginPath();
            ctx.moveTo(60, 362);
            ctx.quadraticCurveTo(256, 312, 452, 362);
            ctx.quadraticCurveTo(256, 412, 60, 362);
            ctx.stroke();
            
            // Draw 3C text
            ctx.fillStyle = 'white';
            ctx.font = 'bold 80px system-ui, -apple-system, sans-serif';
            ctx.textAlign = 'center';
            ctx.textBaseline = 'middle';
            ctx.fillText('3C', 256, 256);
        }
        
        function downloadFavicon(size) {
            // Create a new canvas for the specific size
            const downloadCanvas = document.createElement('canvas');
            downloadCanvas.width = size;
            downloadCanvas.height = size;
            const downloadCtx = downloadCanvas.getContext('2d');
            
            // Get the main canvas
            const mainCanvas = document.getElementById('faviconCanvas');
            
            // Draw the main canvas onto the download canvas, scaled
            downloadCtx.drawImage(mainCanvas, 0, 0, size, size);
            
            // Create download link
            const link = document.createElement('a');
            link.download = `3c-thread-favicon-${size}x${size}.png`;
            link.href = downloadCanvas.toDataURL('image/png');
            
            // Trigger download
            document.body.appendChild(link);
            link.click();
            document.body.removeChild(link);
            
            // Visual feedback
            const buttons = document.querySelectorAll('.btn');
            buttons.forEach(btn => {
                if (btn.textContent.includes(size + 'x' + size)) {
                    const originalText = btn.textContent;
                    btn.textContent = '✅ Downloaded!';
                    btn.style.background = '#10b981';
                    
                    setTimeout(() => {
                        btn.textContent = originalText;
                        btn.style.background = '#3b82f6';
                    }, 2000);
                }
            });
        }
        
        // Add roundRect support for older browsers
        if (!CanvasRenderingContext2D.prototype.roundRect) {
            CanvasRenderingContext2D.prototype.roundRect = function(x, y, width, height, radius) {
                this.moveTo(x + radius, y);
                this.lineTo(x + width - radius, y);
                this.quadraticCurveTo(x + width, y, x + width, y + radius);
                this.lineTo(x + width, y + height - radius);
                this.quadraticCurveTo(x + width, y + height, x + width - radius, y + height);
                this.lineTo(x + radius, y + height);
                this.quadraticCurveTo(x, y + height, x, y + height - radius);
                this.lineTo(x, y + radius);
                this.quadraticCurveTo(x, y, x + radius, y);
                this.closePath();
            };
        }
        
        // Draw favicon when page loads
        window.onload = function() {
            drawFavicon();
        };
    </script>
</body>
</html>

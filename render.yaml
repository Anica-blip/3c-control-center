services:
  - type: cron
    name: scheduled-posts-processor
    env: node
    schedule: "0 6,9,14,17 * * *"
    buildCommand: npm install && npx tsc
    startCommand: node dist/schedulecomponent/api/cronHandler.js
